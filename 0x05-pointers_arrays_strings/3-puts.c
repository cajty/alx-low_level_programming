#include "main.h"

/**
* _puts -that prints a string, followed by a new line.
*@str: char pointer
*
*/
void _puts(char *str)
{
	int a = 0;

	while(s[a])
	{
		a++;
		_putchar(s[a]);
	}
	_putchar('\n');
}
